export const goToMusics = (history) => {
  history.push("/feed");
};

export const goToLogin = (history) => {
  history.push("/");
};

export const goToSignup = (history) => {
  history.push("/signup");
};
